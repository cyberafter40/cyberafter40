/// <reference types="@testing-library/react-native/extend-expect" />

// Registers the typings for RNTL's built-in matchers (toBeDisabled,
// toBeSelected, toHaveAccessibilityValue …) so `npm run typecheck` sees the
// same expect surface the UI suite runs against.
export {};
